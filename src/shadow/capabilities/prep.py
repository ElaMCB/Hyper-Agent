"""Meeting prep — next: calendar + actions + snapshot. Stub for now."""

from pathlib import Path


def run_prep(root: Path, config: dict) -> str:
    return (
        "# Meeting prep\n\n"
        "Not implemented yet. Roadmap: Outlook/Graph or Power Automate + `data/actions.json` + same Snapshot spine.\n"
    )
